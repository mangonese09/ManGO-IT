node_modules/
.env
*-service-account.json
.firebase/
.DS_Store
dist/
build/

__pycache__/
*.pyc

# SAIS raw timetable sweep cache (131MB, refetched on each weekly refresh;
# provenance lives in pipeline/data/sais/manifest.json)
pipeline/data/sais/tt-*.json

# weekly refresh log (local)
pipeline/data/reports/sais-refresh.log

# SAIS Trasporti sweep cache (regenerated per harvest; graph+localities are committed)
pipeline/data/saist/runs-*.jsonl
pipeline/data/saist/sweep*.log
